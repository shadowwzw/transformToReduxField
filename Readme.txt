Данная прога изменяет JSX код.
1) А именно изменяет Field для меток на FieldUI
2) Перечисленные компоненты типа TextBox и т.д переписывает в вариант с redux оберткой.

Символ переноса строк в файле input.txt должен быть LF (Unix)